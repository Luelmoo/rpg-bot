// import { drizzle } from "drizzle-orm/node-postgres";
import { Zone } from "#interfaces/zone";

export default class Exploration {
    // private db = drizzle(process.env.POSTGRESQL_URL!);

    constructor() {}

    public async getResources(zone: Zone) {}
}
